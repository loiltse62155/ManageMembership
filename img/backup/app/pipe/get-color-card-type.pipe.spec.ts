import { GetColorCardTypePipe } from './get-color-card-type.pipe';

describe('GetColorCardTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColorCardTypePipe();
    expect(pipe).toBeTruthy();
  });
});
